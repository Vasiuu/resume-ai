# 1. Move into the project folder you already created
cd resume-ai

# 2. Turn it into a git repo
git init
git add .
git commit -m "feat: initial AI résumé builder with 4 Canva templates"

# 3. Add the remote origin
git remote add origin https://github.com/YOUR_USERNAME/resume-ai.git

# 4. Push to main branch
git branch -M main
git push -u origin main
